{
  "title": "Public Smoke",
  "repository": "GoThruMedia/gothru-maps-frontend",
  "generated_at": "2026-06-05T07:43:21.633320600+00:00",
  "issues": [
    {
      "anchor_id": "issue-gothrumedia-gothru-maps-frontend-114",
      "display_title": "Issue #114: Map Photographer Popup Should Close On Outside Click",
      "main_description_html": "<h3>Description</h3>\n<p>On the <strong>Find Photographers</strong> map, clicking a marker opens a photographer info popup (photo, name, location, \"View Photographer\" button). Currently, clicking anywhere outside the popup does <strong>not</strong> dismiss it. Users expect the popup to close when they click outside of it.</p>\n<img width=\"791\" height=\"465\" alt=\"Image\" src=\"assets/image-e4eda451-255b-4272-9b1d-ae33b0a9b28a.png\" />\n<h3>Current behavior</h3>\n<ul>\n<li>Click a map marker → photographer popup opens.</li>\n<li>Click outside the popup (on the map or surrounding page) → popup stays open.</li>\n<li>The only way to close it appears to be the small \"✕\" in the popup corner.</li>\n</ul>\n<h3>Expected behavior</h3>\n<ul>\n<li>Clicking anywhere outside the popup (map background, page, another area) closes it.</li>\n<li>Clicking the marker / inside the popup keeps it open as today.</li>\n<li>The existing \"✕\" close button continues to work.</li>\n</ul>\n<h3>Notes / context</h3>\n<ul>\n<li>Relevant area: <code>src/components/pages/map/</code> (map surface + photographer popup card).</li>\n<li>The map communicates via a <code>postMessage</code> bridge (<code>mapContainer</code> / <code>mapPostsMessage</code>) — verify whether the popup lives in the React layer or inside the embedded map iframe, since outside-click handling differs:\n<ul>\n<li><strong>React layer:</strong> add an outside-click / dismiss handler (e.g. Radix <code>onPointerDownOutside</code>, or a <code>useOnClickOutside</code> hook).</li>\n<li><strong>Embedded iframe:</strong> the close-on-outside-click must be handled inside the map surface and/or via a postMessage event.</li>\n</ul>\n</li>\n<li>Ensure outside-click dismissal doesn't fire when re-selecting a different marker.</li>\n</ul>\n<h3>Acceptance criteria</h3>\n<ul>\n<li><input disabled=\"\" type=\"checkbox\"/>\nClicking outside the popup closes it.</li>\n<li><input disabled=\"\" type=\"checkbox\"/>\nClicking the popup or its controls does not close it.</li>\n<li><input disabled=\"\" type=\"checkbox\"/>\n\"✕\" close button still works.</li>\n<li><input disabled=\"\" type=\"checkbox\"/>\nSelecting another marker swaps the popup without flicker/double-close.</li>\n</ul>\n<h3>Scenario Testing (for tester)</h3>\n<blockquote>\n<p>Pre-condition: open the <strong>Find Photographers</strong> map page and wait until markers are loaded.</p>\n</blockquote>\n<table><thead><tr><th>#</th><th>Scenario</th><th>Steps</th><th>Expected result</th></tr></thead><tbody>\n<tr><td>1</td><td>Close on outside click (map)</td><td>1. Click a marker to open the popup.<br>2. Click an empty area of the <strong>map</strong> outside the popup.</td><td>Popup closes.</td></tr>\n<tr><td>2</td><td>Close on outside click (page)</td><td>1. Click a marker to open the popup.<br>2. Click an area of the <strong>page outside the map</strong> (e.g. header/list below).</td><td>Popup closes.</td></tr>\n<tr><td>3</td><td>Stay open on inside click</td><td>1. Open the popup.<br>2. Click inside the popup (on the photo, name, or empty popup space — not a link).</td><td>Popup stays open.</td></tr>\n<tr><td>4</td><td>Controls still work</td><td>1. Open the popup.<br>2. Click the <strong>\"View Photographer\"</strong> button.</td><td>Navigates to the photographer profile (popup behaves as today).</td></tr>\n<tr><td>5</td><td>\"✕\" close button</td><td>1. Open the popup.<br>2. Click the <strong>\"✕\"</strong> in the popup corner.</td><td>Popup closes.</td></tr>\n<tr><td>6</td><td>Switch markers</td><td>1. Open the popup for marker A.<br>2. Click a <strong>different</strong> marker B.</td><td>Popup for A closes and popup for B opens — no flicker, no double-close, no popup left stuck open.</td></tr>\n<tr><td>7</td><td>Reopen after outside-close</td><td>1. Open popup, close it via outside click.<br>2. Click the same marker again.</td><td>Popup reopens correctly.</td></tr>\n<tr><td>8</td><td>Mobile / touch</td><td>Repeat scenarios 1, 3, 5, 6 using <strong>tap</strong> on a mobile viewport / touch device.</td><td>Same behavior as desktop — tap outside closes, tap inside keeps open.</td></tr>\n<tr><td>9</td><td>Map interactions not broken</td><td>1. Open the popup.<br>2. Pan and zoom the map by dragging / scroll.</td><td>Map still pans/zooms; an intentional drag should not be mistaken for an outside-click in a way that breaks normal use.</td></tr>\n</tbody></table>\n<p><em>Source: user feedback (screenshot) on the photographer map popup.</em></p>\n<hr />\n<h2>RESULT</h2>\n<p>Implemented close-on-outside-click for the photographer popup.</p>\n<p><strong>Changes:</strong></p>\n<ul>\n<li><code>src/components/main/maps/source/photographer/hooks/usePhotographerMapEvents.ts</code> — call <code>resetPhotographer()</code> in <code>handleMapClick</code> when no photographer marker found at click point (closes popup on empty map click)</li>\n<li><code>src/components/main/maps/ui/PhotographerPopup.tsx</code> — add <code>document.mousedown</code> listener to close popup when clicking outside the map container (React layer)</li>\n<li><code>src/components/development/maps/ui/PhotographerPopup.tsx</code> — same fix applied to dev component</li>\n<li><code>next.config.mjs</code> — add <code>apil.gothru.co</code> to <code>images.remotePatterns</code></li>\n</ul>\n<p>Commit: <code>991af7f1fc1bd2bea7ad47a546cdf9f77e2bf1f1</code></p>\n",
      "metadata": {
        "number": 114,
        "author": "anurdin01",
        "updated_at": "2026-06-05T03:53:03Z"
      }
    }
  ],
  "docs": []
}