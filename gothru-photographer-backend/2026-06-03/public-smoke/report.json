{
  "title": "Public Smoke",
  "repository": "GoThruMedia/gothru-maps-backend-go",
  "generated_at": "2026-06-03T11:20:22.440498100+00:00",
  "issues": [
    {
      "anchor_id": "issue-gothrumedia-gothru-maps-backend-go-18",
      "display_title": "Issue #18: Investigate Photographer Main Location Not Saving",
      "main_description_html": "<h2>Summary</h2>\n<p>Saving a photographer's <strong>main location</strong> appears to succeed — the API returns <code>200</code> with the submitted coordinates — but a subsequent fetch of the location returns the old / empty value. The issue was reported from the GoThru Dashboard, where the developer confirmed it is <strong>not a frontend bug</strong>: the dashboard sends the correct payload and receives a success response, yet the saved location never comes back on read. Investigation should focus on the <strong>GoThru Photographer API</strong> (this repo).</p>\n<p>Source report (Dashboard): GoThruMedia/gothru-dashboard#4061</p>\n<blockquote>\n<p><strong>Developer note (from #4061):</strong> \"After testing it, it seems this is an API issue because while the API returns a success response when saving the address, the location field is still empty when retrieving the data via the API.\"</p>\n</blockquote>\n<h2>Affected endpoints (this repo — <code>gothru-maps-backend-go</code>)</h2>\n<table><thead><tr><th>Action</th><th>Method / Route</th><th>Handler</th><th>Action layer</th></tr></thead><tbody>\n<tr><td>Save</td><td><code>PATCH /v1/photographers/:gothruUserId/location</code></td><td><code>src/controllers/v1/photographers/updatePhotographerLocation.go</code></td><td><code>src/action/v1/photographers/updatePhotographerLocation.go</code></td></tr>\n<tr><td>Read</td><td><code>GET /v1/photographers/:gothruUserId/location</code></td><td><code>src/controllers/v1/photographers/getPhotographerLocation.go</code></td><td><code>src/action/v1/photographers/getPhotographerLocation.go</code></td></tr>\n</tbody></table>\n<h2>Leading hypothesis — stale Redis cache not invalidated on save</h2>\n<p><code>GetPhotographerLocation</code> caches its response under <code>RedisKey.PhotographerLocation</code> → <code>photographer:v{version}:location:{gothruUserId}</code> with a 24h TTL (<code>RedisCacheTTL</code>).</p>\n<p><code>UpdatePhotographerLocation</code> writes to the DB and then clears these patterns:</p>\n<ul>\n<li><code>photographer:v*:detail:*</code></li>\n<li><code>geodata:v*:*</code></li>\n<li><code>photographer:v*:count*</code></li>\n<li><code>photographer:v*:markers*</code></li>\n<li><code>photographer:v*:list-v2*</code></li>\n</ul>\n<p><strong>None of these match <code>photographer:v*:location:*</code>.</strong> So after a successful save, the GET endpoint keeps serving the <em>pre-save</em> cached value (e.g. empty / <code>0,0</code>) for up to 24 hours — which exactly matches the reported symptom. The DB write itself most likely succeeds; the <strong>read is stale</strong>.</p>\n<h3>What to verify (in order)</h3>\n<ol>\n<li>Reproduce with cache <strong>on</strong> vs. cache <strong>bypassed</strong> — <code>GET /v1/photographers/:userId/location?disablecache=true</code>. If the bypass returns the saved coordinates but the normal cached call does not → confirms the cache-invalidation gap.</li>\n<li>Inspect the <code>photographers</code> row directly right after a save to confirm the DB persisted (<code>latitude</code>, <code>longitude</code>, <code>city_id</code>, <code>address_line_1</code>).</li>\n<li>If confirmed, the fix is to also clear <code>PhotographerLocation</code> (and consider <code>PhotographersLocationBySlug</code> / <code>LocationData</code>) inside <code>UpdatePhotographerLocation</code>'s invalidation block.</li>\n</ol>\n<h2>Secondary checks (rule out other causes)</h2>\n<ul>\n<li>Read and write hitting different DB connections / a read replica with lag (<code>GetConnPhotographers()</code>).</li>\n<li>GORM zero-value handling — the update uses <code>Updates(map[string]interface{})</code>, which <em>does</em> write zero values, so <code>0,0</code> is intentionally writable; confirm no hook/scope overrides it.</li>\n<li>Confirm save validation isn't silently changing the value (<code>0,0</code> passes the <code>-90..90</code> / <code>-180..180</code> bounds and is accepted as valid).</li>\n</ul>\n<h2>Test scenarios (for testers)</h2>\n<blockquote>\n<p>Replace <code>:userId</code> with a real photographer's <code>gothruUserId</code>, and use the actual deployed base URL.</p>\n</blockquote>\n<h3>TS-1 — Reproduce the bug (cached read after save)</h3>\n<ol>\n<li><code>GET /v1/photographers/:userId/location</code> → note current <code>latitude</code> / <code>longitude</code>.</li>\n<li><code>PATCH /v1/photographers/:userId/location</code> with body <code>{ \"latitude\": -6.2, \"longitude\": 106.8, \"addressLine1\": \"Jakarta\" }</code>.</li>\n<li>Expect <code>200</code> with <code>{ \"latitude\": -6.2, \"longitude\": 106.8 }</code>.</li>\n<li>Immediately <code>GET /v1/photographers/:userId/location</code>.</li>\n<li><strong>Bug present if</strong> the GET still returns the old / empty coordinates instead of <code>-6.2 / 106.8</code>.</li>\n</ol>\n<h3>TS-2 — Cache-bypass confirms DB persisted</h3>\n<ol>\n<li>Repeat TS-1 steps 1–3.</li>\n<li><code>GET /v1/photographers/:userId/location?disablecache=true</code>.</li>\n<li><strong>Diagnostic:</strong> returns the just-saved coordinates. If the bypass returns the saved value while TS-1's cached GET does not → root cause = cache not invalidated.</li>\n</ol>\n<h3>TS-3 — Direct DB verification</h3>\n<ol>\n<li>After a <code>PATCH</code> save, run <code>SELECT latitude, longitude, city_id, address_line_1 FROM photographers WHERE gothru_user_id = :userId;</code>.</li>\n<li><strong>Expected:</strong> the row reflects the saved values.\n<ul>\n<li>DB has new value but API GET does not → <strong>cache</strong> issue.</li>\n<li>DB also missing the value → <strong>persistence</strong> issue.</li>\n</ul>\n</li>\n</ol>\n<h3>TS-4 — Save then read after cache cleared / TTL</h3>\n<ol>\n<li>Save a new location (TS-1).</li>\n<li>Wait for invalidation (post-fix: immediate) or manually delete the <code>photographer:v*:location:*</code> key.</li>\n<li><code>GET</code> → must return the saved location.</li>\n</ol>\n<h3>TS-5 — Happy path end-to-end (post-fix acceptance)</h3>\n<ol>\n<li>Save valid location → <code>200</code>.</li>\n<li>GET (normal, cached) → returns saved coordinates immediately.</li>\n<li>Navigate away and re-fetch → still correct.</li>\n</ol>\n<h3>TS-6 — Edge: <code>0,0</code> coordinates</h3>\n<ol>\n<li>Save <code>{ \"latitude\": 0, \"longitude\": 0 }</code> → <code>200</code>.</li>\n<li>GET → returns <code>0,0</code> consistently (treated as a valid, intentional value, not \"empty\").</li>\n</ol>\n<h3>TS-7 — Sub-locations unaffected</h3>\n<ol>\n<li>Add / update / delete a sub-location.</li>\n<li><code>GET</code> location → <code>sublocations</code> array reflects changes; main location unchanged.</li>\n</ol>\n<h3>TS-8 — Invalid input rejected</h3>\n<ol>\n<li><code>PATCH</code> with <code>\"latitude\": 200</code> (out of range) → expect <code>400</code> <code>\"latitude must be between -90 and 90\"</code>; no DB change.</li>\n</ol>\n<h3>TS-9 — Unknown user</h3>\n<ol>\n<li><code>PATCH</code> / <code>GET</code> for a non-existent <code>gothruUserId</code> → expect <code>404</code>.</li>\n</ol>\n<h2>Acceptance criteria</h2>\n<ul>\n<li>A photographer can save their main location and <strong>immediately read it back</strong> (normal cached GET) with the saved coordinates.</li>\n<li>DB and API GET stay consistent (ideally immediately after save).</li>\n<li>Root cause is documented (cache vs. persistence) together with the fix.</li>\n<li>Sub-location behavior and input validation are unchanged.</li>\n</ul>\n<h2>References</h2>\n<ul>\n<li>Dashboard issue: GoThruMedia/gothru-dashboard#4061 (includes AC-1..AC-8 describing expected end-to-end save / re-fetch behavior).</li>\n</ul>\n<hr />\n<h2>RESULT</h2>\n<p>Root cause confirmed: <code>UpdatePhotographerLocation</code> was not invalidating the <code>photographer:v*:location:*</code> Redis cache key after a successful DB write. The GET endpoint cached responses with a 24h TTL under <code>PhotographerLocation</code> key, but the update action only cleared <code>detail</code>, <code>geodata</code>, <code>count</code>, <code>markers</code>, and <code>list-v2</code> patterns — none of which match the location key.</p>\n<p><strong>Fix (commit <code>55df328</code>):</strong></p>\n<ul>\n<li>Added <code>PhotographerLocationPattern</code> to <code>src/helpers/redis_keys.go</code> → pattern <code>photographer:v*:location:{identifier}</code></li>\n<li>Added <code>ClearRedisKeys(RedisKey.PhotographerLocationPattern(gothruUserId))</code> call in <code>src/action/v1/photographers/updatePhotographerLocation.go</code> after the DB update</li>\n</ul>\n<p>After the fix, a <code>PATCH /v1/photographers/:gothruUserId/location</code> immediately invalidates the location cache, so the subsequent <code>GET</code> returns the freshly saved coordinates without requiring <code>?disablecache=true</code>.</p>\n",
      "metadata": {
        "number": 18,
        "author": "anurdin01",
        "updated_at": "2026-06-03T07:49:01Z"
      }
    },
    {
      "anchor_id": "issue-gothrumedia-gothru-maps-backend-go-19",
      "display_title": "Issue #19: Handle Issue User List: fix & verify search + filter + pagination (dashboard endpoint)",
      "main_description_html": "<h2>Context</h2>\n<p>Backend support for the dashboard \"Featured Photographer / User List\" screen. Frontend integration is tracked in <a href=\"https://github.com/GoThruMedia/gothru-dashboard/issues/3676#issuecomment-4250487028\" target=\"_blank\" rel=\"noopener noreferrer\">gothru-dashboard#3676</a>, which reports: <em>\"🐞 [Search] Search logic and pagination are not functioning correctly.\"</em> This card covers the <strong>backend</strong> side in this repo.</p>\n<h2>API reference</h2>\n<p><a href=\"https://documenter.getpostman.com/view/42915848/2sBXVhDW8f#b727a157-8391-467a-9cad-186a4ba37ad6\" target=\"_blank\" rel=\"noopener noreferrer\">Postman doc</a></p>\n<pre><code>GET {{base_url}}/v1/photographers/users/dashboard?search=&amp;filter=&amp;size=&amp;page=\n</code></pre>\n<table><thead><tr><th>Param</th><th>Notes</th></tr></thead><tbody>\n<tr><td><code>search</code></td><td>free-text search term</td></tr>\n<tr><td><code>filter</code></td><td><code>all</code>, <code>fullname</code>, <code>email</code>, <code>userid</code>, <code>status</code></td></tr>\n<tr><td><code>size</code></td><td>page size, default 10</td></tr>\n<tr><td><code>page</code></td><td>page number, default 1</td></tr>\n</tbody></table>\n<p>Existing code:</p>\n<ul>\n<li>Action: <code>src/action/v1/photographers/getPhotographersDashboard.go</code></li>\n<li>Controller: <code>src/controllers/v1/photographers/getPhotographersDashboard.go</code></li>\n<li>Route: <code>src/routes/v1/photographersRoutes.go:24</code></li>\n</ul>\n<h2>🐞 Confirmed bug (root cause of the report)</h2>\n<p>In <code>src/action/v1/photographers/getPhotographersDashboard.go</code> (lines 110-113) the total count is run against the <strong>unfiltered</strong> table, not the <code>query</code> that has the <code>search</code>/<code>filter</code> <code>WHERE</code> clauses applied:</p>\n<pre><code class=\"language-go\">if err := db.Model(&amp;models.DirectoryPhotographer{}).Count(&amp;totalPhotographers).Error; err != nil {\n</code></pre>\n<p>So when searching/filtering, <code>pagination.pages</code> reflects the whole table (e.g. 1001 pages) instead of the matched rows — exactly the \"pagination not functioning correctly\" symptom. It should count the filtered <code>query</code> instead.</p>\n<h2>Acceptance criteria</h2>\n<ul>\n<li><input disabled=\"\" type=\"checkbox\"/>\n<code>Count</code> uses the same filtered <code>query</code>, so <code>pagination.pages</code> is correct for any <code>search</code>/<code>filter</code> combination.</li>\n<li><input disabled=\"\" type=\"checkbox\"/>\nAll filter modes verified: <code>all</code>, <code>fullname</code>, <code>email</code>, <code>userid</code>, <code>status</code> (incl. non-numeric input for <code>userid</code>/<code>status</code> returning empty, not the whole table).</li>\n<li><input disabled=\"\" type=\"checkbox\"/>\n<code>search</code> empty + <code>filter=all</code>/empty still served from cache; filtered/search requests bypass cache (current behavior preserved).</li>\n<li><input disabled=\"\" type=\"checkbox\"/>\nPagination (<code>page</code>, <code>size</code>) returns correct slice and total <code>pages</code> count.</li>\n<li><input disabled=\"\" type=\"checkbox\"/>\nResponse shape unchanged (matches Postman sample: <code>slug, avatar, fullName, email, gothruSubscriptionId, gothruUserId, requestDate, status, isFeatured</code>).</li>\n</ul>\n<h2>Files</h2>\n<ul>\n<li><code>src/action/v1/photographers/getPhotographersDashboard.go</code> (fix + tests)</li>\n</ul>\n<hr />\n<h2>RESULT</h2>\n<p>Root cause confirmed: <code>Count</code> in <code>GetPhotographersDashboard</code> ran against bare <code>db.Model(&amp;models.DirectoryPhotographer{})</code> (unfiltered) instead of <code>query</code> (which carries the <code>search</code>/<code>filter</code> WHERE clauses). This caused <code>pagination.pages</code> to reflect the whole table row count regardless of the active filter — exactly the \"pagination not functioning correctly\" symptom.</p>\n<p><strong>Fix (commit <code>c970d27</code>):</strong></p>\n<ul>\n<li><code>src/action/v1/photographers/getPhotographersDashboard.go</code> line 111: changed <code>db.Model(&amp;models.DirectoryPhotographer{}).Count(&amp;totalPhotographers)</code> → <code>query.Count(&amp;totalPhotographers)</code></li>\n</ul>\n<p>Now <code>pagination.pages</code> correctly reflects the filtered result count for all <code>search</code>/<code>filter</code> combinations. Cache behavior (only for empty search + <code>filter=all</code>) unchanged.</p>\n",
      "metadata": {
        "number": 19,
        "author": "anurdin01",
        "updated_at": "2026-06-03T07:52:21Z"
      }
    }
  ],
  "docs": []
}