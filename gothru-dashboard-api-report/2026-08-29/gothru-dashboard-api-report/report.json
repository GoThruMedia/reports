{
  "title": "GoThru Dashboard API Report",
  "repository": "GoThruMedia/GoThru-API-New",
  "generated_at": "2026-08-29T12:36:23.307592100+00:00",
  "issues": [
    {
      "anchor_id": "issue-gothrumedia-gothru-api-new-1794",
      "display_title": "Issue #1794: Record PayPal recurring transactions cronjob",
      "main_description_html": "<h2>Summary</h2>\n<p>PayPal-equivalent of #1764: a cronjob endpoint that records PayPal recurring subscription renewal payments into the legacy billing tables, the same way <code>RecordStripeRecurringTransactionsHelper</code> does for Stripe.</p>\n<h2>Current state</h2>\n<p>Stripe recurring renewal charges are now logged via <code>/record-stripe-recurring-transactions</code> (#1764, PR #1760): each renewal charge is written to <code>cc_logs_stripe</code>, and a deduped <code>cc_purchase</code> row is inserted when the charge succeeded and a <code>user_id</code> was resolved. PayPal REST API subscriptions (<code>CreateSubscription.php</code>) have no equivalent — renewal payments on PayPal subscriptions are not being logged to <code>cc_logs_paypal</code> or <code>cc_purchase</code> anywhere in the API codebase.</p>\n<p>Note: #1471 already tracks a broader PayPal cronjob (porting the legacy PayFlow Pro script <code>get_paypal_link_trans.php</code> <em>and</em> covering REST API subscriptions). This issue scopes out just the REST API recurring-renewal slice, mirrored 1:1 against the Stripe implementation, so it can ship independently of the PayFlow Pro migration.</p>\n<h2>Target state</h2>\n<ul>\n<li>Fetch PayPal subscription renewal payments for the cronjob window and log every attempt to <code>cc_logs_paypal</code>.</li>\n<li>Insert a deduped <code>cc_purchase</code> row for successful renewals when a <code>user_id</code> can be resolved.</li>\n<li>Resolve <code>user_id</code> from <code>user_extra_data.recurring_profile</code> / <code>recurring_profile_photo_studio</code> — same column Stripe uses; PayPal subscription IDs (<code>I-...</code>) are stored there too (see <code>CreateSubscription.php</code>), so the resolution tier is identical to <code>RecordStripeRecurringTransactionsHelper::resolveUserId()</code>.</li>\n<li>Resolve <code>product_id</code> the same way the sync PayPal subscribe flow does (<code>PaypalHelper::convertProductNameToId()</code> / plan-to-product mapping — check <code>CreateSubscription.php</code> for the exact source).</li>\n<li>Dedup purchase inserts by PayPal capture/transaction id.</li>\n<li>Support <code>?test_mode=1</code> against PayPal sandbox, matching the Stripe cronjob's manual-verification pattern.</li>\n</ul>\n<h2>Open design question — how to enumerate renewal payments</h2>\n<p>Stripe's helper does one polling call (<code>charges.all</code> with a <code>created</code> range) across the whole account. PayPal REST has no direct equivalent:</p>\n<ul>\n<li><strong>Option A — per-subscription polling:</strong> loop every active PayPal <code>recurring_profile</code> in <code>user_extra_data</code>, call <code>GET /v1/billing/subscriptions/{id}/transactions?start_time=&amp;end_time=</code> for each. Simple, no extra scopes, but O(n) calls per run.</li>\n<li><strong>Option B — Transaction Search API:</strong> <code>GET /v1/reporting/transactions</code> returns all account transactions (including subscription payments) in one paginated call, closer to the Stripe <code>charges.all</code> shape — but requires the Transaction Search product/scope to be enabled on the PayPal app; confirm this is available before committing to it.</li>\n<li><strong>Option C — webhooks:</strong> subscribe to <code>PAYMENT.SALE.COMPLETED</code> / <code>BILLING.SUBSCRIPTION.PAYMENT.FAILED</code> and log on receipt instead of polling. Real-time, but it's a webhook handler, not a cronjob — bigger shape change from the Stripe pattern and needs its own reconciliation story for missed events.</li>\n</ul>\n<p>Whoever picks this up should confirm which option is viable against the current PayPal app config before implementing.</p>\n<h2>Scope</h2>\n<ul>\n<li><code>routes/cronjobRoutes.php</code> — add the cronjob route (e.g. <code>record-paypal-recurring-transactions</code>).</li>\n<li><code>app/Http/Controllers/Cronjobs/CronjobController.php</code> — wire the endpoint, following <code>recordStripeRecurringTransactions()</code> as the template.</li>\n<li><code>app/Http/Controllers/Cronjobs/Helpers/RecordPaypalRecurringTransactionsHelper.php</code> — orchestrate payment fetch, filtering, logging, user/product resolution, and purchase insert.</li>\n<li><code>app/Http/Controllers/Cronjobs/Helpers/PaypalRecurringChargeRules.php</code> — isolate the recurring-cycle and purchase-recording decision rules, mirroring <code>StripeRecurringChargeRules</code>.</li>\n<li><code>app/Http/Controllers/Payment/Helpers/Paypal/PaypalHelper.php</code> — add whatever fetch method the chosen option (A/B/C above) needs.</li>\n<li><code>api-collection/GoThru API NEW/Cronjob/Record Paypal Recurring Transactions.yml</code> — add Bruno/manual API entry.</li>\n<li><code>tests/Helpers/Cronjobs/PaypalRecurringChargeRulesTest.php</code> — cover the pure decision rules.</li>\n</ul>\n<h2>Out of scope</h2>\n<ul>\n<li>Legacy PayFlow Pro migration (<code>get_paypal_link_trans.php</code>) — tracked separately in #1471.</li>\n<li>Backfilling historical recurring payments.</li>\n<li>Changing existing synchronous subscribe, upgrade, downgrade, Stripe, or non-subscription purchase flows.</li>\n</ul>\n<h2>References</h2>\n<ul>\n<li>Stripe implementation: <code>app/Http/Controllers/Cronjobs/Helpers/RecordStripeRecurringTransactionsHelper.php</code>, <code>StripeRecurringChargeRules.php</code> (#1764, PR #1760)</li>\n<li>PayPal subscription creation flow: <code>app/Http/Controllers/Payment/Actions/Paypal/Subscription/Subscription/CreateSubscription.php</code></li>\n<li>Related broader ticket: #1471</li>\n</ul>\n",
      "metadata": {
        "number": 1794,
        "author": "alfarez21",
        "updated_at": "2026-08-26T10:19:02Z"
      }
    },
    {
      "anchor_id": "issue-gothrumedia-gothru-api-new-1804",
      "display_title": "Issue #1804: Add record-payflow-recurring-transactions cron (separate) to record legacy Payflow Pro (RP...) recurring payments",
      "main_description_html": "<h2>Context</h2>\n<p><code>record-paypal-recurring-transactions</code> only records REST (<code>I-...</code>) subscriptions. Payflow Pro recurring profiles (<code>RP...</code>) are auto-charged but never recorded: <strong>0 of 73 approved recurring since Aug 4</strong> exist in <code>cc_purchase</code>/<code>cc_logs_paypal</code> (legacy cron stopped during migration).</p>\n<h2>Decision: separate cron</h2>\n<p>New route <code>record-payflow-recurring-transactions</code> + own helper. Existing REST cron untouched. Different API (XML report engine vs REST JSON), different keys (dedup by PNREF, user from Comment2 <code>recurring_for_user_{id}</code>, amounts in cents), isolated failure domain. Persistence classes (<code>CCLogClass</code>, <code>CCTransClass</code>, <code>InvoiceHelper</code>, <code>EmailClass</code>) reused. NVP <code>inquireProfile</code> blocked by IP whitelist — report engine works.</p>\n<h2>Product resolution</h2>\n<p>Exact amount match on active PayPal products (<code>plan_paypal IS NOT NULL</code>). Filter removes all ambiguity — validated 98/98 unique:</p>\n<table><thead><tr><th>Amount</th><th>tx</th><th>Product</th></tr></thead><tbody>\n<tr><td>$15</td><td>28</td><td>#14 basic</td></tr>\n<tr><td>$34</td><td>55</td><td>#1 pro</td></tr>\n<tr><td>$75</td><td>10</td><td>#16 agency</td></tr>\n<tr><td>$105</td><td>2</td><td>#18 agency_1</td></tr>\n<tr><td>$195</td><td>1</td><td>#18 agency_4</td></tr>\n<tr><td>$340</td><td>2</td><td>#6 proy</td></tr>\n</tbody></table>\n<p>No unique match → null + <code>skipped_no_product</code>, never guess.</p>\n<h2>Plan</h2>\n<ol>\n<li><code>PayflowRecurringChargeRules</code> — window (24h default, overrides), Comment2→user, PNREF, status <code>Approved</code>, product rule above.</li>\n<li><code>RecordPayflowRecurringTransactionsHelper</code> — counters, dry-run/preview, dedup by PNREF, insert <code>cc_logs_paypal</code> (R) + <code>cc_purchase</code> + invoice.</li>\n<li>Route + <code>CronjobController</code> action.</li>\n<li>First run backfills Aug 4 → today, then daily.</li>\n</ol>\n<h2>Acceptance</h2>\n<ul>\n<li><input disabled=\"\" type=\"checkbox\"/>\nDry-run backfill (Aug 4 → now) previews ~73 tx with correct user/PNREF/product/amount</li>\n<li><input disabled=\"\" type=\"checkbox\"/>\nReal run inserts without duplicates on re-run</li>\n<li><input disabled=\"\" type=\"checkbox\"/>\nExisting REST cron unchanged</li>\n<li><input disabled=\"\" type=\"checkbox\"/>\nNon-approved/unparseable logged + counted, never guessed</li>\n</ul>\n",
      "metadata": {
        "number": 1804,
        "author": "alfarez21",
        "updated_at": "2026-08-28T12:06:03Z"
      }
    },
    {
      "anchor_id": "issue-gothrumedia-gothru-api-new-1805",
      "display_title": "Issue #1805: Add PayPal failed-payment detection to /cronjob/process-unsubscribes-table",
      "main_description_html": "<h2>Summary</h2>\n<p>Extend <code>GET /cronjob/process-unsubscribes-table</code> to also detect genuinely-failed <strong>PayPal REST API</strong> recurring subscriptions and queue them for cancellation (Phase 1 PayPal), reusing the existing provider-agnostic <code>UnsubscribesSweeper</code> (Phase 2, unchanged).</p>\n<p>PayPal does <strong>not</strong> auto-cancel a subscription on failed renewal (unlike Stripe) — a failed payment just leaves it <code>SUSPENDED</code>. This closes that gap.</p>\n<h2>Spec</h2>\n<p><code>docs/superpowers/specs/2026-08-28-paypal-unsubscribes-detection-design.md</code></p>\n<h2>Decisions</h2>\n<ul>\n<li><strong>Failure definition:</strong> PayPal subscription status via <code>PaypalHelper::getSubscriptionDetails()</code> = <code>SUSPENDED</code> / <code>CANCELLED</code> / <code>EXPIRED</code>.</li>\n<li><strong>Candidate scope:</strong> main table only — <code>user_extra_data.recurring_profile LIKE 'I-%'</code> (+ join <code>users</code>, subscribtion ∈ [1,2,5,6,7,8]).</li>\n<li><strong>Window params:</strong> <code>start_date</code>/<code>end_date</code> ignored by PayPal detector (point-in-time status poll).</li>\n<li><strong>Dedup:</strong> shared in-memory <code>$queuedUserIds</code> registry (by reference) across Stripe + PayPal detectors so a user failed on both providers gets at most one pending row per pass (critical in <code>dry_run</code>).</li>\n</ul>\n<h2>Changes</h2>\n<ol>\n<li><strong>New</strong> <code>Cronjobs/Helpers/PaypalFailedPaymentDetector</code> — mirrors <code>StripeFailedPaymentDetector</code> (counters, preview, guards, insert type=2 row).</li>\n<li><strong>New</strong> <code>UnsubscribeRules::isFailedPaypalStatus()</code> — pure rule: <code>in_array($status, ['SUSPENDED','CANCELLED','EXPIRED'], true)</code>.</li>\n<li><strong>Edit</strong> <code>CronjobController::processUnsubscribesTable()</code> — wire shared registry + new detector + new response key <code>phase_1_paypal_detect</code>.</li>\n<li><strong>Edit</strong> <code>StripeFailedPaymentDetector</code> — minimal refactor to use shared registry (no behavior change).</li>\n</ol>\n<h2>Response shape (new)</h2>\n<p><code>phase_1_detect</code> (Stripe, unchanged), <code>phase_1_paypal_detect</code> (new), <code>phase_2_sweep</code> (unchanged).</p>\n<h2>Testing</h2>\n<ul>\n<li>Unit: <code>isFailedPaypalStatus()</code> (status gating, strict-case).</li>\n<li>Unit: detector candidate-query semantics.</li>\n<li>Manual (PayPal sandbox, <code>test_mode=1</code>): SUSPENDED → row inserted; second run → dedup; <code>dry_run</code> preview zero-writes; ACTIVE → skipped.</li>\n</ul>\n",
      "metadata": {
        "number": 1805,
        "author": "alfarez21",
        "updated_at": "2026-08-28T13:58:21Z"
      }
    }
  ],
  "docs": []
}